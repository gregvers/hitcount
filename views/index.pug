extends layout

block content
    h1= title
    p This page was served by #{hostname}
    p Date and Time: #{datetime}
    p Number of hits on each App Server:
    ul
      for res in results
        li= res.appserver + ' = ' + res.hit
